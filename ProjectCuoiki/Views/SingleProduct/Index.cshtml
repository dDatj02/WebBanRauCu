@model ProjectCuoiki.Models.product
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Default/Mylayout.cshtml";
}

<!-- breadcrumb-section -->
<div class="breadcrumb-section breadcrumb-bg">
    <div class="container">
        <div class="row">
            <div class="col-lg-8 offset-lg-2 text-center">
                <div class="breadcrumb-text">
                    <p>See more Details</p>
                    <h1>Single Product</h1>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- end breadcrumb section -->
<!-- single product -->
<div class="single-product mt-150 mb-150">
    <div class="container">
        <div class="row">
            <div class="col-md-5">
                <div class="single-product-img">
                    <img src="~/Uploads/img/products/@Model.imgname" alt="">
                </div>
            </div>
            <div class="col-md-7">
                <div class="single-product-content">
                    <h3>@Model.name</h3>
                    <p class="single-product-pricing"><span>Per Kg</span> $@Model.price.GetValueOrDefault().ToString("#,###", System.Globalization.CultureInfo.GetCultureInfo("vi-VN")) VND</p>
                    <p>@Html.Raw(Model.description)</p>
                    <div class="single-product-form">
                        <form action="@Url.Action("AddCart","Cart")" method="post">
                            <input type="number" placeholder="1" id="quantity" min="1" value="1" name="quantity">
                            <input hidden value="@Model.id" name="idProduct" />
                            <button type="submit" class="cart-btn"><i class="fas fa-shopping-cart"></i> Add to Cart</button>
                        </form>
                        <a type="submit" class="cart-btn"><i class="fas fa-shopping-cart"></i> Add to Cart</a>

                        <p><strong>Categories: </strong>Fruits, Organic</p>
                    </div>
                    <h4>Share:</h4>
                    <ul class="product-share">
                        <li><a href=""><i class="fab fa-facebook-f"></i></a></li>
                        <li><a href=""><i class="fab fa-twitter"></i></a></li>
                        <li><a href=""><i class="fab fa-google-plus-g"></i></a></li>
                        <li><a href=""><i class="fab fa-linkedin"></i></a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- end single product -->
<!-- more products -->
<div class="more-products mb-150">
    <div class="container">
        <div class="row">
            <div class="col-lg-8 offset-lg-2 text-center">
                <div class="section-title">
                    <h3><span class="orange-text">Related</span> Products</h3>
                    <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Aliquid, fuga quas itaque eveniet beatae optio.</p>
                </div>
            </div>
        </div>
        <div class="row">
           @{Html.RenderAction("RelatedProduct", "SingleProduct", new { meta = Model.typeproduct.meta, id=Model.id });}
        </div>
    </div>
</div>
<!-- end more products -->
@*<script>
    function addcart(idProduct) {
        quantity = $("#quantity").val()
        $.ajax({
            url: '"@Url.Action("AddCart", "Cart")',
            type: 'get',
            data: { idProduct: idProduct, quantity: quantity },
            success: function (data) {
                alert("Đã thêm vào giỏ hàng")
            },
            error: function (data) {
                alert("Lỗi")
            }
            })
    }
</script>*@